server.address=0.0.0.0
# Puerto del servidor
server.port=8080

# Clave usada por tu ApiKeyFilter (app key interna). Por defecto para pruebas:
app.api.key=${APP_API_KEY:api_key_progra}

# Clave para DeepSeek (recomiendo usar variable de entorno DEEPSEEK_API_KEY)
# Valor por defecto s�lo para pruebas locales (reemplaza o borra en prod)
deepseek.api.key=sk-75f25927aa6a4a19b33a4d1e2906d437

# (Opcional) Si a�n necesitas la clave de Gemini mantenla aqu� (pero no se usar� si migras)
gemini.api.key=${GEMINI_API_KEY:}

# Logging
logging.level.root=INFO
logging.level.com.tuorg.calcservice.service=DEBUG

# Jackson / zona horaria (opcional)
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss.SSSZ
spring.jackson.time-zone=UTC
